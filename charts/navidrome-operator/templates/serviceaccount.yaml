{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "navidrome-operator.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "navidrome-operator.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
